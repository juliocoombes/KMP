
/*import './Investments.css';
import ContactSection from '../contactSession/ContactSection';
import contenido from '../../data/contenido';

export default function Investments() {
  const { inversiones } = contenido;

  return (
    <section className="investments-section">
      <h2 className="section-title">{inversiones.titulo}</h2>
      <p className="section-subtitle">{inversiones.subtitulo}</p>

      <div className="investments-grid">
        {inversiones.items.map((item, index) => (
          <div className="investment-card" key={index}>
            <img src={item.icono} alt={item.titulo} className="investment-icon" />
            <h3>{item.titulo}</h3>
            <p>{item.descripcion}</p>
          </div>
        ))}
      </div>

      <a href={inversiones.cta.link} className="cta-button mt">
        {inversiones.cta.texto}
      </a>

      <ContactSection />
    </section>
  );
}*/
